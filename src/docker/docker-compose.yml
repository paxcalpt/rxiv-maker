# RXiv-Maker Docker Compose - Optimized Configuration
# Provides multiple services for different use cases with efficient resource usage

version: '3.8'

services:
  # =====================================
  # Production Service - PDF Generation
  # =====================================
  rxiv-maker:
    build:
      context: ../../
      dockerfile: src/docker/Dockerfile
      target: production
      args:
        BUILDKIT_INLINE_CACHE: 1
    image: rxiv-maker:latest
    container_name: rxiv-maker-prod
    volumes:
      # Mount only necessary directories for production
      - ../../:/app:ro  # Read-only for security
      - ../../output:/app/output:rw  # Read-write for output
      - rxiv-maker-cache:/tmp  # Temporary cache volume
    working_dir: /app
    environment:
      - PYTHONPATH=/app/src/py
      - PYTHONUNBUFFERED=1
    networks:
      - rxiv-maker-net
    restart: "no"
    # Resource limits for efficiency
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: '2.0'
        reservations:
          memory: 512M
          cpus: '0.5'

  # =====================================
  # Development Service - Interactive
  # =====================================
  dev:
    build:
      context: ../../
      dockerfile: src/docker/Dockerfile
      target: development
      args:
        BUILDKIT_INLINE_CACHE: 1
    image: rxiv-maker:dev
    container_name: rxiv-maker-dev
    volumes:
      # Full read-write access for development
      - ../../:/app:rw
      - rxiv-maker-cache:/tmp
      - dev-home:/home/appuser  # Persistent dev environment
    working_dir: /app
    environment:
      - PYTHONPATH=/app/src/py
      - PYTHONUNBUFFERED=1
      - DEVELOPMENT=1
    networks:
      - rxiv-maker-net
    stdin_open: true
    tty: true
    command: tail -f /dev/null
    # Development resource limits
    deploy:
      resources:
        limits:
          memory: 4G
          cpus: '4.0'

  # =====================================
  # Watch Service - Auto-rebuild
  # =====================================
  watch:
    build:
      context: ../../
      dockerfile: src/docker/Dockerfile
      target: development
    image: rxiv-maker:dev
    container_name: rxiv-maker-watch
    volumes:
      - ../../:/app:rw
      - rxiv-maker-cache:/tmp
    working_dir: /app
    environment:
      - PYTHONPATH=/app/src/py
      - PYTHONUNBUFFERED=1
    networks:
      - rxiv-maker-net
    command: make watch
    restart: unless-stopped

  # =====================================
  # Test Service - CI/CD Testing
  # =====================================
  test:
    build:
      context: ../../
      dockerfile: src/docker/Dockerfile
      target: development
    image: rxiv-maker:dev
    container_name: rxiv-maker-test
    volumes:
      - ../../:/app:ro
      - rxiv-maker-cache:/tmp
    working_dir: /app
    environment:
      - PYTHONPATH=/app/src/py
      - PYTHONUNBUFFERED=1
    networks:
      - rxiv-maker-net
    command: make test
    profiles:
      - testing

# =====================================
# Networks and Volumes
# =====================================
networks:
  rxiv-maker-net:
    driver: bridge
    name: rxiv-maker-network

volumes:
  rxiv-maker-cache:
    name: rxiv-maker-cache
    driver: local
  dev-home:
    name: rxiv-maker-dev-home
    driver: local
